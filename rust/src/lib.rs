//! DS&A implementations in Rust.
//!
//! Add modules per topic under `data_structures/` and `algorithms/`, then declare
//! them here. Tests live inline in each module behind `#[cfg(test)]` and run with
//! `cargo test`.

pub mod algorithms;
pub mod data_structures;
